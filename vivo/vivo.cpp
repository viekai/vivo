#include "log.h"
#include "common.h"
#include "dexfile.h"
#include <string>
#include <ostream>

#include "classloader.h"

int main(int argc, const char* argv[]) {
    // find the main Class
    // vivo::ClassLoader* boot_class_loader = new ClassLoader("main.dex");
    // vivo::Class* main_class = boot_class_loader.FindClass("Switch");
    return 0;
}
